---

---

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const toggle = document.getElementById("themeToggle");
    const circle = document.getElementById("themeCircle");
    const html = document.documentElement;

    if (!toggle || !circle) return;

  
    if (
      localStorage.theme === "dark" ||
      (!("theme" in localStorage) && window.matchMedia("(prefers-color-scheme: dark)").matches)
    ) {
      html.classList.add("dark");
      toggle.classList.add("bg-red-600");
      toggle.classList.remove("bg-gray-200");
      circle.classList.add("translate-x-5");
    }

    toggle.addEventListener("click", () => {
      const isDark = html.classList.toggle("dark");

      if (isDark) {
        toggle.classList.add("bg-red-600");
        toggle.classList.remove("bg-gray-200");
        circle.classList.add("translate-x-5");
        localStorage.theme = "dark";
      } else {
        toggle.classList.add("bg-gray-200");
        toggle.classList.remove("bg-red-600");
        circle.classList.remove("translate-x-5");
        localStorage.theme = "light";
      }
    });
  });
</script>

<header class="header fixed w-full top-0 left-0 z-50 border-b py-8 px-7 bg-white dark:bg-gray-900 dark:border-gray-700 transition-colors duration-300">
  <div class="flex items-center justify-between">

    <div class="flex items-center space-x-2">
      <img src="/KINASIS-logo.png" alt="Logo KINASIS" class="w-9 h-9" />
      <span class="brand-title text-brand-red text-lg font-semibold tracking-wide">KINASIS</span>
    </div>

    <div class="flex items-center">
      <nav class="hidden md:flex space-x-6 nav-links mr-5">
        <a href="/sobre-nosotros" class="nav-link">Sobre nosotros</a>
        <a href="/nuestro-equipo" class="nav-link">Nuestro equipo</a>
        <a href="/servicios" class="nav-link">Servicios</a>
        <a href="/proyectos" class="nav-link">Proyectos</a>
        <a href="/contacto" class="nav-link">Contacto</a>
      </nav>

      <div class="flex items-center space-x-3">
        <a
          href="https://github.com/"
          target="_blank"
          rel="noopener noreferrer"
          class="bg-[#2B2B2B] text-white px-4 py-1.5 rounded hover:bg-black transition-colors"
        >
          GitHub
        </a>


        <button
          id="themeToggle"
          aria-label="Cambiar tema"
          class="relative w-10 h-5 bg-gray-200 rounded-full transition-colors duration-300 flex items-center"
        >
          <span
            id="themeCircle"
            class="absolute left-1 w-3.5 h-3.5 bg-white rounded-full transition-all duration-300 transform"
          ></span>
        </button>
      </div>
    </div>
  </div>
</header>